# @poyoapi/mcp

stdio bridge to `https://api.poyo.ai/mcp`.

```bash
POYO_API_KEY=... npx @poyoapi/mcp
```

Optional filters:

- `POYO_MCP_CATEGORIES` or `--categories`
- `POYO_MCP_MODELS` or `--models`

Configure categories or models, never both.
